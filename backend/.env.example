DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=xxx
DB_NAME=car_sales
REDIS_HOST=localhost
REDIS_PORT=6379
FASTAPI_PORT=8001
# 日志：DEBUG / INFO / WARNING / ERROR（根 logger 控制台阈值；普通文件仍为 INFO+ 且不含 ERROR）
LOG_LEVEL=INFO
# 可选，默认 <仓库根>/logs
# LOG_DIR=/var/log/car-sales
